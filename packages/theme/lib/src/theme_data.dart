import 'package:flutter/material.dart';

import 'text_theme.dart';

ThemeData baseThemeData({required ColorScheme colorScheme}) => ThemeData(
  useMaterial3: true,
  colorScheme: colorScheme,
  textTheme: textTheme,
  // toggleButtonsTheme:
);
